@using lab2.ViewModel

@model TraineeCourseCrsresult
@{
    ViewData["Title"] = "GetREsult";
}

@* <h1>Result</h1>
 *@


<div class="container mt-5">
<div class="card mx-auto shadow" style="max-width: 600px;">
    

    <div class="card-body text-center">

        <h4 class="mb-2"><strong>trainee name:</strong> @Model.Trainee_name</h4>
        <h4 class="mb-2"><strong>course name:</strong> @Model.crs_name</h4>
            <h4 class="mb-2"><strong>Degree:</strong> @Model.Degree </h4>
            <h4 class="mb-2">
                <strong>statuse:</strong>
                <span class="@(Model.statuse == "Successful" ? "text-success" : "text-danger")">
                    @Model.statuse
                </span>
            </h4>

    </div>


</div>
</div>
